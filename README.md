# bootstraps-y2

All projects bootstraps (Epitech 2nd year)

## Flow

On new bootstrap, create new branch with project name:

```sh
git switch -c my_project
```

then once the bootstrap is finished, merge onto main.
